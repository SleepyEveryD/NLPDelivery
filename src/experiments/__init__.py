"""Adaptive prompt-routing research package.

Self-contained from the live game it is -- isolates ONE variable (the prompt strategy) so the
"does adaptive routing beat a universal prompt?" question, cleanly we can answer.
"""
